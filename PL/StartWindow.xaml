<Window x:Class="PL.StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Style="{StaticResource BackGroundPic}"
        ResizeMode="NoResize"
        Icon="{StaticResource WindowIcon}"
        Title="Log In" Height="430" Width="300"
     
         DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid Margin="20">
        <!-- Define rows and columns -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title Label -->
        <Label Content="Log In" FontSize="30" Height="40" 
               FontWeight="Bold" HorizontalAlignment="Center" Grid.ColumnSpan="2" Grid.Row="0"/>

        <!--image logo-->
        <Image Source="Images/HelpOnRoad.png" Width="200" Height="150" Grid.Row="1" Grid.ColumnSpan="2"/>

        <!-- Id Label -->
        <Label Content="Id: " VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="2"/>

        <!-- TextBox for ID input -->
        <TextBox Text="{Binding logInId}" Grid.Row="2" Grid.Column="1" Margin="5,5,5,5"/>

        <!-- Password Label -->
        <Label Content="Password: " VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="3"/>

        <!-- TextBox for Password input -->
        <TextBox Text="{Binding password}" Grid.Row="3" Grid.Column="1" Margin="5,5,5,5"/>

        
        <!-- Log In Button -->
        <Button Content="Log In" Click="btnLogIn_Click" Width="150" Height="40" 
                HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="4" Grid.ColumnSpan="2"/>
    </Grid>
</Window>
